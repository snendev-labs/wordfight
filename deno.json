{
  "imports": {
    "jsr:@std/http": "jsr:@std/http"
  },
  "tasks": {
    "build": "trunk build --release",
    "serve": "deno run -A webserver.ts",
    "run-server": "cargo run -p wordfight-server --release",
    "clippy": "cargo clippy --workspace --all-targets --all-features -- --deny warnings"
  }
}
